# Stock Auto Trader Roadmap

이 문서는 현재 mock 기반 프로토타입 이후 진행해야 할 작업을 정리한다. 실거래 기능은 안전장치와 검증이 완료되기 전까지 `TRADING_MODE=paper` 기준으로만 개발한다.

## 1. 브로커 연동

- 사용할 증권사 API를 확정한다.
- API 인증 방식, 토큰 갱신 방식, 요청 제한, 장 운영 시간 정책을 문서화한다.
- `BrokerClient` 인터페이스를 기준으로 실제 브로커 어댑터를 추가한다.
- 계좌 요약, 보유 종목, 현재가 조회, 주문 접수 기능을 paper 모드에서 먼저 검증한다.
- API 장애, 인증 만료, 요청 제한 초과, 네트워크 실패에 대한 오류 처리를 추가한다.

완료 기준:

- `/api/broker/status`에서 실제 브로커 연결 상태를 확인할 수 있다.
- paper 모드에서 실제 계좌/시세 조회가 동작한다.
- 주문 API는 live 모드가 아니면 실거래 주문을 보내지 않는다.

## 2. 주문 안전장치

- [x] 종목별 최대 주문 금액과 최대 수량 제한을 추가한다.
- [ ] 동일 종목 중복 주문 방지 규칙을 추가한다.
- [ ] 장 시작 전, 장 마감 후 주문 차단 규칙을 추가한다.
- [x] 시장가/지정가 주문별 필수 입력값 검증을 강화한다.
- [x] live 모드 전환 시 명시적인 확인 절차를 추가한다.

완료 기준:

- [x] 위험한 주문 요청은 브로커 호출 전에 차단된다.
- [ ] 차단 사유가 API 응답과 UI에 명확히 표시된다.
- [x] live 모드 주문은 별도 설정 없이는 실행되지 않는다.

## 3. 전략 구조 개선

- 샘플 전략과 실사용 전략을 분리한다.
- 전략 입력값에 시세 이력, 보유 포지션, 주문 이력, 현금 비중을 포함한다.
- 전략별 설정값을 환경 변수나 설정 파일로 주입할 수 있게 한다.
- 전략 실행 결과에 매수/매도 근거, 신뢰도, 리스크 정보를 포함한다.

완료 기준:

- 새로운 전략을 `TradingStrategy` 구현체로 쉽게 추가할 수 있다.
- 전략 결과가 주문 실행과 UI 표시 모두에 충분한 정보를 제공한다.

## 4. 백테스트와 시뮬레이션

- 과거 가격 데이터 입력 형식을 정한다.
- 전략을 과거 데이터에 반복 적용하는 백테스트 엔진을 추가한다.
- 수익률, 최대 낙폭, 승률, 거래 횟수, 수수료 반영 결과를 계산한다.
- 시뮬레이션 UI에서 백테스트 결과를 확인할 수 있게 한다.

완료 기준:

- 최소 1개 종목에 대해 샘플 전략 백테스트를 실행할 수 있다.
- 백테스트 결과가 JSON뿐 아니라 화면에서 요약된다.

## 5. 데이터 저장

- 주문 이력, 전략 실행 이력, 에러 로그 저장 방식을 결정한다.
- 초기 단계에서는 SQLite 또는 파일 기반 저장소를 검토한다.
- 저장 레이어 인터페이스를 분리해 추후 DB 교체가 가능하게 한다.
- 주요 API 응답과 주문 요청을 감사 로그로 남긴다.

완료 기준:

- 전략 실행과 주문 요청 이력이 재시작 후에도 남는다.
- 장애 분석에 필요한 최소 로그를 조회할 수 있다.

## 6. UI 개선

- 브로커 연결 상태, 계좌, 포지션, 전략 실행 결과를 분리된 화면으로 정리한다.
- 주문 실행 전 확인 화면을 추가한다.
- 오류, 로딩, 빈 상태를 명확히 표시한다.
- 전략별 설정값을 UI에서 조정할 수 있게 한다.

완료 기준:

- 사용자가 현재 모드가 paper인지 live인지 항상 확인할 수 있다.
- 주문 실행 전 종목, 수량, 금액, 주문 타입을 다시 확인할 수 있다.

## 7. 테스트와 품질 관리

- 브로커 mock과 전략 엔진 단위 테스트를 추가한다.
- API 라우트 입력 검증 테스트를 추가한다.
- 주문 안전장치 테스트를 추가한다.
- CI에서 lint, type check, build, test를 실행한다.

완료 기준:

- 핵심 주문 흐름은 자동 테스트로 검증된다.
- GitHub Actions에서 기본 품질 검사가 통과한다.

## 8. 배포와 운영

- 배포 환경을 결정한다.
- 환경 변수 관리 방식을 정한다.
- paper 모드 운영 대시보드를 먼저 배포한다.
- live 모드 전환 전 운영 체크리스트를 만든다.

완료 기준:

- 배포된 환경에서 paper 모드로 전체 흐름을 실행할 수 있다.
- live 모드 전환 조건과 롤백 절차가 문서화되어 있다.

## 우선순위

1. 실제 브로커 API 선정
2. 브로커 어댑터 구현
3. 주문 안전장치 구현
4. 전략 입력 구조 확장
5. 백테스트 엔진 추가
6. 테스트와 CI 구성
7. UI 개선
8. 배포 준비
