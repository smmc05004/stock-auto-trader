# AI 작업 시작 규칙

이 파일은 이 저장소 전체에 적용된다. 새 세션이나 대화 초기화 후에도 작업 전에 읽는다. 대화 기억보다 저장소의 문서와 실제 Git·서버 상태를 근거로 판단한다.

## 반드시 먼저 확인할 것

1. [작업 및 이미지 배포 규약](docs/development-and-deployment.md)을 읽는다.
2. `git status --short --branch`, 현재 브랜치, 최근 커밋, 관련 PR 상태를 확인한다. 기존 미커밋 변경을 덮어쓰거나 다른 작업과 무조건 합치지 않는다.
3. 코드 변경 전 관련 전략 문서와 구현을 확인한다. 배포 작업이면 `.github/workflows/`, `Dockerfile.paper`, Compose, systemd 설정까지 확인한다.
4. 문서에 적힌 계획을 구현 완료로 간주하지 않는다. 배포 상태는 실행 이미지 digest·Git SHA·서버 보고서로 확인한다. 이전 IP와 임시 SSH 키가 여전히 유효하다고 가정하지 않는다.

## 작업 흐름

- **main에서 직접 수정·커밋하지 않는다.** `feat/...`, `fix/...`, `docs/...`, `chore/...` 작업 브랜치를 먼저 만든다. 기존 작업 브랜치에서는 해당 작업을 이어간다.
- 변경 → 관련 검사 → 커밋 → PR → 필수 CI 통과 → main 머지 순서로 진행한다. main 직접 푸시와 검사 우회를 정상 절차로 사용하지 않는다.
- 작업 종료 시 브랜치·커밋·검사 결과·PR/머지 여부·배포 여부를 실제 상태대로 알린다. 커밋, 머지, 이미지 업로드, EC2 적용은 각각 다른 단계다.
- 문서 작성 요청을 자동으로 인프라 변경·머지·배포 요청으로 확대하지 않는다. 사용자가 이미 승인한 작업 범위는 불필요하게 재확인하지 않는다.

## 배포 원칙

- **승인된 목표: main 머지 → GitHub Actions 검사·이미지 빌드 → ECR → EC2 이미지 자동 배포.** GitHub OIDC와 AWS Systems Manager를 사용하고 이미지 digest로 버전을 고정한다.
- **2026-09-16 문서 작성 시점: 위 자동 배포는 아직 미구현.** 현재 CI는 검사만 수행하며 Compose에는 EC2 빌드 설정이 남아 있다. 구현 후 상세 문서의 상태 표를 근거와 함께 갱신한다.
- 미커밋 파일·작업 브랜치 소스를 EC2로 복사하거나 서버에서 임의 수정해 운영에 반영하지 않는다. 기존 소스 업로드·EC2 빌드 절차를 새 표준으로 반복하지 않는다. 자동 배포가 미구현이면 먼저 구현 상태를 설명하고 요청된 범위에서 작업한다.
- 거래 중 컨테이너를 무조건 재시작하지 않는다. 신규 진입 중지 → 기존 주문·포지션 관리 → 증권사 기준 보유·미체결 0 확인 → 백업·교체 순서를 구현한다. 안전 상태를 확인하지 못하면 배포를 보류한다.
- EC2의 환경설정과 SQLite 볼륨을 보존한다. `docker compose down -v` 등 데이터 삭제를 배포 절차로 사용하지 않는다. DB를 과거 백업으로 덮어써 주문 기록을 되돌리지 않는다.
- 실행기 두 개를 동시에 주문 가능 상태로 운영하지 않는다. 서버가 꺼져 있으면 배포 대기로 기록하고 다음 부팅 시 적용하도록 한다.

## 투자 및 비밀정보

- 현재 허용 범위는 **KIS 모의투자**다. `TRADING_MODE=paper`, `ALLOW_LIVE_TRADING=false`를 유지한다. 실전 전환은 별도의 명시적 사용자 지시가 필요하다.
- 계좌 키·토큰·`.env`·SSH 개인키·거래 DB·백업을 커밋하거나 이미지에 포함하지 않는다. 로그와 PR에도 비밀 값을 출력하지 않는다.
- 전략 조건이나 위험 한도 변경은 관련 전략 문서에도 기록해 이후 분석이 가능하게 한다.
